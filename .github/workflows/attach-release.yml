name: Attach v1.6 release assets
on:
  workflow_dispatch:
    inputs:
      tag:
        default: 'v1.6'
permissions:
  contents: write
jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: gh release upload "${{ inputs.tag }}" SoterCloak_v1.6.zip SoterCloak_v1.6_source.zip --repo gongkuan2023-commits/SoterCloak --clobber
